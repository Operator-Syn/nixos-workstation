diff --git a/bol/auth.py b/bol/auth.py
index 7807434..cf20454 100644
--- a/bol/auth.py
+++ b/bol/auth.py
@@ -133,7 +133,7 @@ class NativeAuth:
     def signed_in(self):
         return msa_signed_in()
 
-    def start(self, on_auth=None, on_online=None):
+    def start(self, on_auth=None, on_online=None, on_error=None):
         if msa_signed_in():
             if on_online:
                 on_online()
@@ -145,10 +145,10 @@ class NativeAuth:
         # while this exact generation is still current.
         account_epoch = _account_cache_epoch(DATA / "winegdk-preauth")
         threading.Thread(target=self._flow,
-                          args=(on_auth, on_online, account_epoch),
+                          args=(on_auth, on_online, on_error, account_epoch),
                           daemon=True).start()
 
-    def _flow(self, on_auth, on_online, account_epoch=None):
+    def _flow(self, on_auth, on_online, on_error=None, account_epoch=None):
         try:
             if account_epoch is None:
                 account_epoch = _account_cache_epoch(
@@ -184,8 +184,17 @@ class NativeAuth:
                     interval += 5
                     continue
                 if e:
-                    die(f"Microsoft sign-in failed: "
-                        f"{t.get('error_description') or e}")
+                    detail = t.get("error_description") or e
+                    lowered = str(detail).lower()
+                    if e in {"too_many_attempts", "too_many_requests"} \
+                            or "too many" in lowered:
+                        die("Microsoft sign-in was rate-limited. Close the "
+                            "login dialog, wait for Microsoft's cooldown, "
+                            "then request one fresh code.")
+                    if e in {"expired_token", "bad_verification_code"}:
+                        die("Microsoft sign-in code expired or was rejected. "
+                            "Request a fresh code and try again.")
+                    die(f"Microsoft sign-in failed: {detail}")
                 if t.get("refresh_token"):
                     token = {"refresh_token": t["refresh_token"],
                              "obtained": int(time.time())}
@@ -201,11 +210,27 @@ class NativeAuth:
                     ok("Microsoft account linked (in-game login)")
                     return
             if not self._stop:
-                warn("Microsoft sign-in timed out — click 'Sign in' again.")
-        except BolError:
-            pass
+                message = "Microsoft sign-in timed out — request a fresh code."
+                warn(message)
+                if on_error:
+                    try:
+                        on_error(message)
+                    except Exception:
+                        pass
+        except BolError as exc:
+            if on_error:
+                try:
+                    on_error(str(exc))
+                except Exception:
+                    pass
         except Exception as ex:
-            err(f"Native login error: {ex}")
+            message = f"Native login error: {ex}"
+            err(message)
+            if on_error:
+                try:
+                    on_error(message)
+                except Exception:
+                    pass
 
     def stop(self):
         self._stop = True
diff --git a/bol/gui.py b/bol/gui.py
index 9b53770..96efcbd 100644
--- a/bol/gui.py
+++ b/bol/gui.py
@@ -537,6 +537,7 @@ class AuthBridge(QObject):
     gets the callback to run.
     """
     auth = Signal(str, str)
+    error = Signal(str)
     online = Signal()
     refreshed = Signal()
 
@@ -1036,6 +1037,7 @@ class MainWindow(QMainWindow):
         # safely no matter which thread emits them.
         self._auth_bridge = AuthBridge()
         self._auth_bridge.auth.connect(self._on_auth)
+        self._auth_bridge.error.connect(self._on_auth_error)
         self._auth_bridge.online.connect(self._on_online)
         self._auth_bridge.refreshed.connect(self._on_refreshed)
 
@@ -1769,7 +1771,8 @@ class MainWindow(QMainWindow):
             threading.Thread(
                 target=lambda: self.na.start(
                     self._auth_bridge.auth.emit,
-                    self._auth_bridge.online.emit),
+                    self._auth_bridge.online.emit,
+                    self._auth_bridge.error.emit),
                 daemon=True).start()
 
     # ---------------------------------------------------------- download account
@@ -1880,6 +1883,14 @@ class MainWindow(QMainWindow):
         self._refresh_account_row("auth")
         self._code_dialog(url, code)
 
+    def _on_auth_error(self, message):
+        if not _alive(self):
+            return
+        _desktop_error(message)
+        if getattr(self, "_auth_dialog", None) and _alive(self._auth_dialog):
+            self._auth_dialog.close()
+        self._refresh_account_row("in" if msa_signed_in() else "out")
+
     def _on_online(self):
         # Reached via AuthBridge.online, already queued onto the UI thread.
         if not _alive(self):
@@ -1940,7 +1951,7 @@ class MainWindow(QMainWindow):
         threading.Thread(target=work, daemon=True).start()
 
     def _code_dialog(self, url, code):
-        full_url = f"https://login.live.com/oauth20_remoteconnect.srf?otc={code}"
+        full_url = url or "https://www.microsoft.com/link"
         dlg = QDialog(self)
         dlg.setObjectName("Root")
         dlg.setWindowTitle("Sign in to Microsoft")
